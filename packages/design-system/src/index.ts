/* The package's public surface. Primitives arrive in Task 3, the grade card in
   Task 4. Until then the package ships styles only, and `export {}` keeps this
   a module rather than a script. */
export {};
