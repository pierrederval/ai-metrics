# Fieldnote preferred visual baseline

User selected this direction on 2026-09-07, before logo exploration.

Open `preferred-baseline.html` directly in a browser. It is a self-contained visual prototype with illustrative data, not a production integration.

Selected: Integrated Air Glass page, Current solid menu, page gradient starting at the menu color, subtle menu right border (#BFCFC4), Pine & Mist palette, Original Ember accent (#BE421F), restrained menu accent, outlined fn seal, Georgia display type and Arial interface text. The selected option opens by default. The Current comparison remains available.

Preserve this file unchanged as the return point for future explorations.

## Selected logo — 2026-09-07

The user approved option 3, Field Lines, after refining all three turns to be rounded and evenly spaced. `approved-field-lines.html` preserves the selected logo with the preferred Integrated Air Glass design and opens option 3 by default. `field-lines.svg` is the standalone vector mark: Original Ember #BE421F, 3-unit strokes, 8-unit centerline spacing, concentric corner radii 20/12/4, rounded ends, aligned endpoints.

The earlier `preferred-baseline.html` remains unchanged as the pre-logo-exploration return point. These are approved visual references, not a deployed app implementation.
